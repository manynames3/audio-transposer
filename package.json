{
  "name": "audio-transposer",
  "version": "1.0.0",
  "private": true,
  "description": "Static browser app for transposing authorized audio files and direct audio URLs.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-07-05",
    "deploy": "wrangler pages deploy public --project-name audio-transposer",
    "check": "node --check public/app.js && node --check functions/api/fetch-media.js && node scripts/check-static.js",
    "test": "npm run check"
  },
  "keywords": [
    "audio",
    "transpose",
    "cloudflare-pages"
  ],
  "license": "UNLICENSED"
}
